api_platform:
    mapping:
        paths: ['%kernel.project_dir%/src/Entity']
    patch_formats:
        json: ['application/merge-patch+json']
    swagger:
        versions: [3]
    
    # Configuration des routes avec le préfixe /apiplatform
    defaults:
        path_prefix: /apiplatform
